# Verification: Меню v2

---

## Статус: ✅ PASS (нет блокирующих противоречий)

---

## Проверки

### V-1: Авто-триал vs. кнопка триала в меню

**Проверка:** REQ-1.1 (авто-триал на /start) + C-2.1 (убрать кнопку триала из меню) — не противоречат ли?

**Вывод:** ✅ Консистентно. Триал выдаётся автоматически → кнопка «Получить триал» в меню больше не нужна и убирается. `is_trial_available` больше не влияет на состав кнопок главного меню.

---

### V-2: «Заполнить профиль» vs. бонус один раз

**Проверка:** REQ-2.2 (FSM запускается) + REQ-2.3 (бонус один раз) — как guard реализован?

**Вывод:** ⚠️ ОТКРЫТЫЙ ВОПРОС. Нужно проверить, есть ли в модели `User` поле-guard для onboarding bonus (например `onboarding_bonus_granted`). Если нет — убедиться, что FSM онбординга уже содержит такую проверку. Если поля нет → нужна миграция. Агент должен проверить перед реализацией.

---

### V-3: Кнопка «Оплатить» vs. «Продлить» в подписке

**Проверка:** REQ-4.* говорит о кнопке «Оплатить» в главном меню, AC-3.3 говорит о «Продлить» внутри «Моя подписка» — дублирование?

**Вывод:** ✅ Не противоречие, а два входа в один флоу. «Оплатить» (NavSubscription.PAY) — прямой вход. «Продлить» (NavSubscription.EXTEND) внутри подписки — тот же destination. Оба валидны.

---

### V-4: Multi-admin — Config vs. IsAdmin

**Проверка:** C-4.1 говорит что IsAdmin уже поддерживает список. Проверено в коде: `cls.admins_ids: list[int]` и `env.list("BOT_ADMINS", subcast=int)` — оба уже списки.

**Вывод:** ✅ Изменение только в `.env` на сервере. Код не меняется.

---

### V-5: 3x-ui — диагностика vs. реализация

**Проверка:** REQ-8.1/8.2 описывают добавление сервера через /admin_server. Существует ли этот handler?

**Вывод:** ✅ `NavAdminTools.SERVER_MANAGEMENT`, `ADD_SERVER`, `СONFIRM_ADD_SERVER` — все есть в navigation.py. Handler в `app/bot/routers/admin_tools/`. Нужно только проверить работоспособность через диагностику, не переписывать.

---

### V-6: «Инструкции» — заглушка vs. NavDownload.MAIN

**Проверка:** REQ-7.2 говорит «заглушка», но NavDownload.MAIN существует и может вести на платформенный флоу.

**Вывод:** ⚠️ Агент должен проверить текущий handler для NavDownload.MAIN. Если он пытается показать реальные инструкции (QR, ссылки) → или временно заменить ответ на заглушку, или добавить пустой роут. Контент инструкций — out of scope.

---

### V-7: Платёжные флаги vs. существующий код

**Проверка:** C-3.2 — выставить флаги в `.env`. Проверить что subscription handler действительно читает эти флаги.

**Вывод:** ✅ REQ-3.2 (из v1 спеки) подтверждает: методы фильтруются по `PAYMENT_*_ENABLED`. Нужно только обновить `.env`.

---

## Открытые вопросы к агенту (до реализации)

| # | Вопрос | Действие |
|---|--------|---------|
| OQ-1 | Есть ли в User поле onboarding_bonus_granted? | `grep -r "onboarding_bonus\|bonus_granted" app/db/models/user.py` |
| OQ-2 | NavDownload.MAIN handler — что сейчас возвращает? | Читать `app/bot/routers/download/handler.py` (или аналог) |
| OQ-3 | Server таблица — есть ли записи? | `sqlite3 app/data/db.sqlite3 "SELECT * FROM server;"` на сервере |
| OQ-4 | Текущий `/start` — вызывает ли уже `_try_auto_gift_trial()`? | Читать middleware или handler `/start` |
